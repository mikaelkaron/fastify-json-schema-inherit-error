This repo contains two examples in separate branches:

- `fastify` fastify example using `@fastify/swagger` and `@fastify/swagger-ui`
- `platformatic` platformatic example with a standalone `service` and a `composer`
